#include <iostream>
#include <cmath>

int main(){
    double x = 3;
    double y = 4;

    double z;

    /*
       std::max and min included in iostream header file 
    */
    //z = std::max(x, y);
    //z = std::max(x, y);

    /*
       Below are included in cmath
       pow(x, y)
       sqrt(x)
       abs(-x)
       round(x)
       ceil(x);
       floor(x);

    */
    z = pow(2,4);


}
