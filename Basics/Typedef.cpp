#include <iostream>
#include <vector>

typedef std::string text_t;
using number_t = int; // Using has replaces typedef for the most part on more compatible.

int main(){
    text_t firstName = "Kevin";

    number_t age = 20;

    return 0;
}